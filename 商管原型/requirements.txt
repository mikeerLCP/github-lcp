# 小牛b商管系统 依赖清单
# 安装方式：pip install -r requirements.txt

# ── 核心依赖（必需） ──
python-dateutil>=2.8.0      # 日期计算（月度/季度偏移）

# ── 可选依赖 ──
matplotlib>=3.5.0           # 看板图表（折线图、饼图、条形图）
python-docx>=0.8.0          # Word 文档生成（催缴函）
Flask>=2.0.0                # 商户 Web 门户
